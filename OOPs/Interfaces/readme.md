# Interfaces in Dart #

* Every class implicity defines an interface. But you must reimplement all the methods.

* Abstract class can have both implemented and non-implemented methods, and is used for shared implementation